parameters:
  kapitan:
    compile:
      - input_paths:
          - podsecuritypolicy/component/app.jsonnet
        input_type: jsonnet
        output_path: .
      - input_paths:
          - podsecuritypolicy/component/main.jsonnet
        input_type: jsonnet
        output_path: podsecuritypolicy/
